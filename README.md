# Vulnerable-Web-app
A banking application to teach users about cyber security concepts.


python -m venv venv       # create their own venv
source venv/bin/activate  # activate it
pip install -r requirements.txt